#!/bin/sh

set -e

case "$1" in
 install|upgrade)
  #deleting old kernel
  rm -fr /boot/kernel.img.2013*
  # Backup old kernel.img
  mv /boot/kernel.img /boot/kernel.img.$(date +%Y%m%d)
 ;;
 abort-upgrade)
 ;;
 *)
  exit 1
 ;;
esac
